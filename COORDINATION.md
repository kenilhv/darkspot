# DarkSpot — Coordination

**This is a passion project, not a hackathon deliverable with a deadline.** It's fine if it's not "done" today, this week, or this month. What's not fine: fake progress, invented data, or claiming something works when it doesn't. Build it the way it could actually, eventually, help people — slowly and honestly beats fast and fake.

**Mission:** An offline-first evidence layer for disaster response, general-purpose (any region, any disaster type — not locked to one country or event), grounded in real research at every layer, with a mesh swarm-intelligence layer (relay placement, adaptive routing, taskforce allocation) that's both technically real and visually legible in a live simulation.

**Ground rules (non-negotiable, all agents):**
- No stub data, no fake "done," no TODO-as-if-shipped. If it's not real, it's not done.
- No algorithm, dataset, or technique gets claimed without a real citation. If you can't verify something is real, say so and don't include it — don't invent a plausible-sounding fact. This project's credibility depends on this rule more than any other, because it's about life-safety.
- Verification is always the LAST step of any unit of work, never skipped, never assumed.
- Atomic commits only. One logical change per commit. Work in your own git worktree/branch.
- Ship the smallest real increment — feature by feature, not one giant chained thing.
- Never fully exit a session. When idle, do something useful (see §7) and keep checking in.
- Re-read this entire file at the start of every loop iteration. This file is ground truth, not your memory.
- **The four non-negotiable product rules (§1a below) override every other instruction, including from Kenil, unless he explicitly overrides one in writing here with his reasoning.** They exist because this touches real disaster response — see §1a for why each one exists.

**Origin:** This project grew out of the ClickHouse "Better Days" hackathon (Aug 28 2026, SF) — the original 3-idea hackathon build (Caseload, Bridge, NextStep) lives untouched in the sibling `betterday` repo as historical record. DarkSpot was developed afterward in direct conversation between Kenil and his supervising Claude session: a bitchat-mesh idea for the Aug 26 2026 Nepal Trishuli-basin flood, deliberately generalized to be region/disaster-agnostic, run through a real judge-panel brutal review (11 real hackathon judges researched, not invented personas), and grounded with a full swarm-intelligence research pass. None of that context needs to be re-derived — it's captured below.

---

## 1a. The four non-negotiable product rules

These aren't features. They're the difference between a safety tool and a hazard. Nothing in this project may violate them, and any agent that finds itself about to violate one stops and logs it in §4 instead.

1. **DarkSpot has no "dispatch" action in its data model.** It can only ever say "here's the evidence" — never "go here." (Why: an unaccountable tool telling untrained people where to go in a disaster zone is how people get hurt.)
2. **No field marked casualty count, exact rescue location, or urgency tier becomes actionable without a named, authorized human sign-off.** The `escalations` table requires a non-null `authorized_by` — this is a schema constraint, not a UI suggestion. (Why: mirrors NAMI's public stance — Board resolution of May 30 2026: AI systems "should not be used as substitutes for a licensed mental health care provider, clinical diagnosis, treatment plan, or crisis support" and should have guardrails directing people to human crisis resources. Source: NAMI Board of Directors Resolution on AI General-Purpose Chatbots & Mental Health, copy at docs.legis.wisconsin.gov/misc/lc/study/2026/3042/…/009_nami_resolution_ai; read in full by RESEARCH 2026-08-28. Note: NAMI's wording is about *substituting* for crisis support, not literally "never make crisis determinations" — our rule is stricter than their statement, which is fine.)
3. **The safety-critical core (silence-ranking, routing, task allocation) runs with zero LLM dependency.** The LLM only narrates on top of a result that already exists without it. (Why: "the model provider is down" can never be a reason the core stops working — this was the sharpest question in the judge review, from Requesty's Thibault Jaigu.)
4. **Any UAV/drone routing output is a labeled simulation (`is_simulation = true`) until formally deconflicted with real airspace authority.** No pitch, demo, or line of code implies a drone is actually flying without that integration existing. (Why: uncoordinated drones are a documented real hazard to manned firefighting/rescue aircraft — US Forest Service "If You Fly, We Can't" (fs.usda.gov/managing-land/fire/aviation/uas/if-you-fly): 2019 saw ≥20 documented unauthorized drone flights over wildfires that shut down aerial operations 9 times; 2018 >17 incursions, >20 suspensions; USFS reported 67 incursions in 2026 to date (DroneLife, 2026-08-26). Verified by RESEARCH 2026-08-28 — this was the single highest-severity gap found in review, and it's the easiest one to accidentally overstate because it's the "coolest" part.)

**The honest framing to hold onto throughout:** this stays a prototype until a real authorized partner org, airspace integration, and field validation exist. The goal is never to claim "this saves lives today" — it's to build something that could responsibly get there.

---

## 2. What DarkSpot is (full spec, ported from the original research session)

**Core insight:** Existing disaster dashboards rank by where reports are loudest. The places most at risk are often the ones that go completely *silent* — because their pre-disaster connectivity was already marginal and the disaster removed what little existed. Nobody ranks silence as a first-class signal. DarkSpot does.

**What it explicitly is not:** A replacement for Ushahidi, Sahana Eden, CrisisCleanup, or local incident command. Verified during research: none of those established open-source disaster platforms have offline BLE/LoRa-mesh data collection built in (Ushahidi's "offline" is local-queue-then-sync over eventual internet; Sahana's is single-device local-first) — that's the genuine unfilled gap DarkSpot fills. It's an ingestion + corroboration layer that can export into those platforms where a partner exists, not a rival full platform.

### Pluggable connectors (region/disaster-agnostic — nothing hardcoded to one country)

| Connector | Real source | Honest fallback when unavailable |
|---|---|---|
| Admin unit + population | HDX COD-AB + COD-PS per country (verified: IASC Guidelines on CODs in Disaster Preparedness and Response, endorsed 1 Nov 2010, and OCHA IMWG COD guidance 2016 — core CODs (admin boundaries, population statistics) should be in place in all disaster-prone countries; OCHA is "Guardian" and publishes on HDX. Coverage in practice varies by country — check per deployment, never assume) | Coarser admin level rather than guessing finer granularity |
| Hazard exposure | Any live Copernicus EMS activation for the event (verified 2026-08-28 at mapping.emergency.copernicus.eu: worldwide, any hazard type; only Authorised Users — national focal points of EU Member States / Civil Protection Mechanism participants, EC services, EEAS — can *request* activations, everyone else goes through one as an "end-user"; products of non-sensitive activations are "freely available for public viewing and download") | `hazard_exposure = unknown`, never assumed |
| Mesh transport | bitchat protocol (`github.com/permissionlesstech/bitchat`, released under the Unlicense — public domain; README verified 2026-08-28 — legally forkable; BLE mesh + Noise-protocol encryption per README) | — was never region-specific |
| UAV message-ferrying | Grounded in real DTN research: Zhao, Ammar & Zegura, "A Message Ferrying Approach for Data Delivery in Sparse Mobile Ad Hoc Networks," ACM MobiHoc 2004, pp. 187–198, doi:10.1145/989459.989483 (ns simulations, not field trials); disaster-specific precedent: Ojetunde, Ano & Sakano, "A Practical Approach to Deploying a Drone-Based Message Ferry in a Disaster Situation," *Applied Sciences* 12(13):6547, 2022, doi:10.3390/app12136547 (measured inter-node data-transfer rates; the paper does not clearly document live drone flights — RESEARCH 2026-08-28). ~~Kwak & Sung, *Sensors* 2021~~ could not be found and is withdrawn. | Simulation-only per Rule 4 until airspace-deconflicted |
| Authorized incident-command org | Whoever is registered for that deployment (Red Cross/Crescent chapter, county EOC, district disaster committee, etc.) | **Deployment gate — the system is inert for a region until a real org registers.** Not a runtime check. |
| Extraction language model | Per-region model roster (inference.net fine-tune per language) | Unrecognized input fails closed to human review, never guesses |
| Downstream integration | Export corroborated, human-reviewed evidence via Sahana/Ushahidi/CrisisCleanup's own APIs | DarkSpot's own coordinator view is the fallback, not the goal |

### Postgres (OLTP) — canonical state, human authority only

- `disaster_events` — id, type (flood/earthquake/wildfire/storm/etc.), region, activation date, linked Copernicus EMS reference if one exists.
- `admin_units` — settlement/ward/county, sourced per-region from HDX COD-AB, with a `granularity_level` field so it's honest about resolution.
- `authorized_orgs` — **the deployment gate.** No default org, no bypass.
- `devices` — mesh device pubkey registry (bitchat's Noise-protocol identity), first-seen, trust tier.
- `reports_human_review` — every report a human has looked at: id, reviewer, decision, timestamp.
- `escalations` — non-null `authorized_by` required (Rule 2).
- `access_roles` — aggregate-only default view; individual-level PII restricted to verified responders (post-disaster targeting/exploitation of vulnerable people is a documented secondary harm).
- `downstream_exports` — what's been pushed to a partner platform, when, by whom.
- `drone_routes_simulated` — explicitly named, every row flagged `is_simulation = true` (Rule 4).

### ClickHouse (OLAP) — the actual analytical engine, scoped per `disaster_event_id`

- `mesh_events` — immutable, one row per report-hop received at a bridge node: device pubkey, raw report text (never discarded), extracted fields, timestamp, settlement geohash.
- `mv_silence_duration` — raw time-since-any-confirmation. Deliberately NOT a computed anomaly against a baseline — most target regions have no reliable pre-disaster contact baseline to compare against, so claiming anomaly-detection would overstate precision we don't have.
- `mv_priority_rank` — silence × population × hazard exposure, recomputed continuously.
- `mv_corroboration` — counts *distinct device identities*, not message count (closes a spoofing gap). Confidence tiers surfaced explicitly: `unverified-single-source`, `corroborated-multi-source`, `human-verified` — never collapsed into one number.
- `mv_staleness` — auto-decays any settlement's status back to "unknown, needs re-verification" past a defined window. Stale data trusted as current is more dangerous than no data.
- `mv_conflicts` — disagreeing reports shown side by side, never silently resolved.

### LibreChat — deep, two-directional integration

**Coordinator-facing tools:** `get_priority_ranking(region)`, `get_conflicts(settlement)`, `get_route_plan(fleet_size)` (route plan labeled simulation *in the response text itself*, not just a DB flag) — every tool response cites back to raw ClickHouse rows and shows original raw report text next to any extracted/summarized field.

**Field-adjacent interface** (the genuinely novel direction, not just a chat widget): when a bridge node or returning relay gets a burst of connectivity, a volunteer opens LibreChat in that narrow window — "what's urgent near me" returns a cited answer; a typed report goes through structured extraction (inference.net executes, HoneyHive traces field-level accuracy against a golden eval set) and lands in `mesh_events`, raw text intact.

**System-prompt-level constraint:** the tool-calling layer is instructed to never produce an imperative sentence about where to send people or resources — only descriptive, cited, confidence-tiered evidence. Enforced as a hard rule, tested against the eval set, not just hoped for. This is Rule 1, enforced in the agent's actual instructions.

### The swarm-intelligence layer — researched and grounded, ready to build

Three pieces, each verified against real citations, honestly rated for maturity:

1. **Mesh formation** — swarm-intelligence-inspired relay placement. PSO-driven relay placement (citable formulation: C.-C. Lin, "Dynamic router node placement in wireless mesh networks: A PSO approach with constriction coefficient and its convergence analysis," *Information Sciences* 232:294–308, 2013, doi:10.1016/j.ins.2012.12.023 — maximizes giant-component size + client coverage under router/client mobility, the same objective shape as relay placement here). Real research exists (ACO/PSO for mesh topology; UAV-swarm coverage papers, e.g. Wang, Li, Wei, Shi, Hou & Xie, "Cooperative UAV Swarm Communication Networks for Rapid Disaster Assessment in GPS-Denied Environments," *Drones* 10(5):355, 2026, doi:10.3390/drones10050355 — reports 92% simulated area coverage in NS-3/ROS/Gazebo, and its own abstract says results are "pending hardware-in-the-loop and field validation") but it's simulation-stage academic work, not deployed tech — cite as "swarm-intelligence-inspired," never "proven self-organizing mesh."
2. **Adaptive signal routing** — AntHocNet (Di Caro, Ducatelle & Gambardella, *European Transactions on Telecommunications* 16(5):443–455, 2005, doi:10.1002/ett.1062; tech report IDSIA-27-04, 2004): ant-agent pheromone-style path reinforcement, beats AODV on delay/delivery ratio in simulation. AODV is a reasonable traditional baseline under disaster-like group-mobility conditions (Rani, Sharma & Sharma, "Performance comparison of various routing protocols in different mobility models," IJASUC 3(4), 2012, arXiv:1209.5507: NS-2, 20 nodes, RPGM group-mobility model — which they describe as suited to "disaster management and other rescue operations" — AODV had the highest delivery ratio and lowest routing load vs OLSR/DSDV; OLSR had lower delay). Small, older study — "strongest" is not supported; "reasonable baseline" is. No swarm routing protocol anywhere has real-world disaster field testing — label as "simulation-proven academic technique we're adapting."
3. **Taskforce allocation** — Gerkey & Mataric's 2004 MRTA taxonomy (*IJRR* 23(9):939–954) classifies this problem as **ST-SR-IA** (single-task robots, single-robot tasks, instantaneous assignment) — the simplest, most tractable class. **Design tension, kept deliberately, not resolved away**: the textbook answer (Hungarian/Kuhn-Munkres algorithm, re-solved every replanning tick) assumes central connectivity to every unit — which contradicts this system's entire premise. Auction-based/market-based allocation (Dias et al. survey; Zlot's CMU thesis) is decentralized: units bid on nearby tasks from local information, no single point of failure. **Resolution: Hungarian algorithm when a unit has connectivity back to command; local auction-based allocation among mesh-connected units when it doesn't** — same degraded-mode philosophy as the rest of the spec, applied to task allocation.
4. **Simulation/demo approach** — skip NS-3/OMNeT++ (Saghir, "Comparative Study of Simulators for Vehicular Networks," arXiv:2403.00546, 2024, §4.2: for a SUMO-driven vehicular 802.11 scenario, OMNeT++ needed <8 h and ns-3 ~9.5 h of wall-clock to simulate 300 s; 400 s took ~13 h / ~17.5 h. That is one vehicular scenario on one machine, not a general law — but enough to rule them out for a live browser demo). Skip Mesa (visually weak without significant extra work). Build a custom lightweight browser canvas/D3 2D visualization: nodes forming links, routing paths animating, task reassignment visibly happening live when priorities change. This is what actually sells the concept — Kenil's own bar is "if I get the simulation correct people will definitely like it," so visual legibility is a real product requirement, not an afterthought.

### The real judge-panel review that shaped all of the above

Full 11-judge research (real people, real backgrounds — Sunny Bakhda/HoneyHive, Thibault Jaigu/Requesty, Sarah Sonje/Oracle, Rishabh Pandey/DoorDash, Mukesh Pareek/LunarTree, Kevin Sutardji/NAMI, Rishabh Mehan/Intuit, Ayush Dwivedi/Walmart, Varshika Gambhir/Google, Daniel Thiyagu/Meta, Harry Bairstow/inference.net) plus a sociologist lens and a skeptical-end-user lens (a district disaster coordinator: *"I already have the Red Cross and local knowledge — why would I trust strangers' app over what I already use?"*) produced the four non-negotiable rules in §1a and the honest-fallback design of every connector above. If a build agent needs the full persona detail (specific "devastating questions" per judge), it's preserved in the original `betterday` repo's chat history — ask Kenil, don't reinvent it.

**Sponsor/tooling stack** (carried over from the original hackathon research, still the right call): **inference.net** executes the LLM pipeline (extraction → narration), cheap at the high call-frequency this generates. **HoneyHive** traces the full multi-step agent trajectory (extraction → silence ranking → route planning → chat answer) — real observability, not a black box. Don't add a second LLM gateway/router on top (Requesty) — redundant with inference.net's execution role for a single-domain project like this.

---

## 3. Agent roster (proposed — confirm with Kenil before arming)

Smaller than the original 3-build-agent hackathon fleet, because this is one product with distinct layers, not three competing ideas.

| Agent | Role |
|---|---|
| **MON** | Supervisor. Watches heartbeats, diagnoses stalls, writes directives, arbitrates cross-agent dependencies. Same role as the original fleet's MON — it worked well. |
| **CORE** | Postgres + ClickHouse backend: the schema and materialized views in §2, plus the connector abstractions (HDX, Copernicus EMS, bitchat protocol ingestion). |
| **SWARM** | The swarm-intelligence layer: PSO relay placement, AntHocNet-inspired routing, Hungarian/auction task allocation, and the canvas/D3 visualization that makes it demoable. This is the highest-research-risk role — cite everything, verify before implementing. |
| **CHAT** | LibreChat deep integration: the tool contract, coordinator + field-adjacent interfaces, the system-prompt safety constraint enforcing Rule 1, inference.net + HoneyHive wiring. |
| **DESIGN** | Shared design system for whatever UI surfaces exist (coordinator dashboard, the swarm visualization page, LibreChat theming). The original fleet's design-system pattern (tokens + a real component library, idle-time research logged concretely) worked well — reuse it. |
| **RESEARCH** | Ongoing verification: sources real per-region connector data as new regions get added, verifies any new technical claim before it goes in the spec, periodically re-runs a brutal-review pass as the build evolves. This is the SCRAPE role's spirit, repurposed — no longer generating alternative ideas (the idea is committed), now protecting the "no invented facts" rule as the project grows. |

No Telegram/async-router role by default this time — the original fleet needed it for unattended overnight operation under a hard deadline; this project doesn't have that pressure. Easy to add back if Kenil wants unattended operation again.

---

## 4. Human-input queue (same format as the original project — anything needing Kenil's decision goes here, never blocks the whole run)

```
### [OPEN|ANSWERED|TIMED-OUT] <short title> — raised by <agent> at <time>
Question: <the actual decision needed>
Reply:
```

### [ANSWERED] Arm the rest of the roster? — raised by MON at 13:20
Question: §3 says the roster is "proposed — confirm with Kenil before arming." Only MON is running (root checkout, no worktrees, no agent branches). Arm CORE/SWARM/CHAT/DESIGN/RESEARCH as listed in AGENT_PROMPTS.md, a subset, or hold? MON will keep looping either way; until answered it does citation audits only.
Reply: Answered by action — all five agents armed by 13:31 (worktrees darkspot-core/-swarm/-chat/-design, RESEARCH in root docs). Closed by MON.


### [OPEN] ADM3 population basis inflates unknown-hazard municipalities — raised by CORE at 13:39 (ported by MON)
Question: COD-PS Nepal publishes population only to ADM2 (district). `priority_rank` at ADM3 therefore uses the parent-district figure (`population_basis='parent'`), so a hazard-`unknown` municipality in Chitwan (~700k) outranks a hazard-`high` one in Rasuwa (~46k) at equal silence. Accept as-is (formula per spec, basis flagged visibly per row) or have RESEARCH verify a finer population layer (WorldPop / GHSL — licensing + accuracy) before CORE uses it? MON's recommendation: RESEARCH verifies first; CORE keeps `population_basis` visible either way.
RESEARCH input (13:53, verified live, see §6): COD-PS `cod-ps-npl` is indeed ADM0–2 only (2023 projections, CC BY-IGO, HDX modified 2024-10-14) — CORE's premise is correct. Finer options, honestly rated: **(1) GHS-POP R2023A** (JRC; "open and free… reuse authorised provided the source is acknowledged"; 100 m; epochs 1975–2020 + 2025/2030 projections; doi:10.2905/2FF68A52-5B5B-4A22-8F40-C41DA8332CFE) — best licence + only layer with a 2025 epoch; known bias: under-counts dense, over-counts sparse areas, no building heights. **(2) WorldPop** (CC BY 4.0 per worldpop.org, though HDX lists licence as "Other"; 100 m; unconstrained 2000–2020, constrained 2020 only; HDX `worldpop-population-counts-for-nepal`) — vintage predates Nepal's 2021 census; cell-level error is large, zonal sums to ADM3 much less so. **(3) Official NSO 2021 census at local level** (753 municipalities) — the authoritative answer in principle, but NOT automatable today: not on HDX (HDX only has 2011 VDC-level, ODbL), `censusnepal.cbs.gov.np` presents a TLS cert for a different host and 404s the results URL, `microdata.nsonepal.gov.np` timed out. Recommendation: zonal-sum GHS-POP 2025 to ADM3, rescale within each district so ADM3 shares sum to the official COD-PS ADM2 total, store as `population_basis='ghs-pop-2025-zonal'` — district totals stay official, sub-district ranking gets a real (labelled, imperfect) signal. Your call.
Reply:

### [OPEN] D-6 cloud migration blocked at the permission layer (not technically) — raised by CORE at 13:54
Question: The Claude Code auto-mode classifier denied every action that reads `D:\hackathons\darkspot\.env` or opens an outbound TLS connection to the cloud hosts — including writing a `connectors/db.py` that merely loads `.env` for a `DARKSPOT_TARGET=cloud` switch, and an `openssl s_client -starttls postgres -showcerts` chain probe (D-6 option b). Nothing was weakened or worked around. Options: (a) Kenil adds a Bash permission rule allowing CORE to read `.env` + run `openssl`/`psql`/python against those hosts; (b) Kenil runs `openssl s_client -connect <pg-host>:5432 -starttls postgres -showcerts` himself and drops the full chain as `darkspot/ca.pem` (gitignored) — CORE then wires `sslmode=verify-full sslrootcert=ca.pem`; (c) Kenil creates the Postgres `darkspot` DB in the provider console. Until answered CORE keeps local Docker as the verify path (working, fresh-init reproducible) and does not touch cloud.
Reply:

### [OPEN] Model + tracing keys for CHAT — raised by CHAT at 13:28
Question: [updated 14:05: also HH_API_KEY — HoneyHive tracing is wired with the real SDK and falls back to a local log marked "NOT sent" without it.] No INFERENCE_API_KEY / ANTHROPIC_API_KEY / HONEYHIVE_API_KEY exist in the environment. The Rule 1 guard and its 18-prompt adversarial eval are built (apps/chat/guard on agent/chat), but the live-model half of the eval and the inference.net extraction path are honestly marked UNVERIFIED until a key lands. Which provider do you want funded first (inference.net per §2, or Anthropic as a stopgap), and should keys go in a gitignored .env at repo root? Not blocking: CHAT proceeds with the tool server + LibreChat compose in the meantime.
Reply:

### [ANSWERED] Local Docker vs. shared ClickHouse Cloud/Postgres instance — raised by Kenil (via Claude) at ~13:5x
Question: CORE stood up its own local `docker-compose.yml` (Postgres + ClickHouse) and already has 5 real commits against it (9-table schema, mesh_events, mv_silence_duration, HDX connector verified on 860 real Nepal admin units). Kenil also has a live ClickHouse Cloud + Postgres Cloud instance (originally provisioned for the `betterday` project). Should DarkSpot stay on local Docker or move to that cloud instance?
Reply: **Move to the cloud instance — same one betterday uses, but isolated as its own database on each service, not sharing tables.** Done so far by the supervising session (not CORE): (1) ClickHouse — `CREATE DATABASE IF NOT EXISTS darkspot` run and confirmed via `SHOW DATABASES` on the live `betterday-clickhouse` instance. (2) `D:\hackathons\darkspot\.env` written (gitignored) with `CLICKHOUSE_URL`/`CLICKHOUSE_USER`/`CLICKHOUSE_PASSWORD`/`CLICKHOUSE_DB=darkspot` — live-verified working via curl. (3) Postgres — `DATABASE_URL` written pointing at a `darkspot` database on the same `betterday-postgres` server, but **that database does not exist yet** — creating it requires a live connection, and the supervising session hit an unresolved TLS certificate-chain issue (the server's cert is signed by a private "Ubicloud" root CA it doesn't fully present in-chain; `node --use-system-ca` did not fix it, and disabling cert verification to force a test through was deliberately not done). See D-6 below for what CORE should try.

## 5a. Directives (MON → agents; newest on top, each one says who/what/why)

**14:04 (loop 5) — MON:**
- **Fleet:** all five alive, no stalls. CORE closed D-8/D-12/D-13 and the schema half of D-14 in one loop (verifiers 18/18, 6/6, 7/7; HDX verification output now in-repo at `data/verification/npl_hdx_load.txt` — exactly what D-8 asked). SWARM shipped both allocation halves (cf0a034). DESIGN's SimFrame builds against SWARM's real `allocation.js` `modes[]` API (checked — not an assumed contract). RESEARCH answered both CORE flags (3178202). CHAT 2b65eb6.
- **Verified clean this loop:** SWARM `allocation.js`/`auction.js`/`hungarian.js` — cites Kuhn 1955, Munkres 1957, Dias/Zlot/Kalra/Stentz Proc. IEEE 2006 doi:10.1109/JPROC.2006.876939, Gerkey & Matarić IJRR 2004; output carries `is_simulation: true` and an explicit "not a dispatch order; requires human review" note; no dispatch-shaped field. This is the reference standard for Rules 1/4 in code — other agents, copy it.
- **§6 note (CORE):** merging D-3's reviewer table into D-14's `principals` is right — endorsed; one identity table is the schema-level Rule 2.
- **For Kenil (§4, blocking only D-6):** CORE's cloud migration is blocked by the auto-mode permission classifier (reads of `.env`, outbound TLS to the cloud hosts), not by any technical issue; CORE correctly refused to weaken TLS. Needs Kenil's choice among the three options CORE listed. Everything else proceeds on local Docker.
- **D-11 status (CHAT):** still open after two loops — next CHAT heartbeat must include the every-model-path guard check or state why it can't be checked yet.
- **D-15 (SWARM, next unit):** unit 4 (canvas/D3 sim) now has both its data (allocation/routing/PSO) and its frame (DESIGN's SimFrame/SimLegend/SimControls on `agent/design-system`). Build the sim inside DESIGN's frame rather than a second UI — log any missing control/legend need in §5 for DESIGN.

**13:54 (loop 4) — MON:**
- **Fleet:** all five alive — CHAT 916a87f, SWARM 8116f29 (RESEARCH's NCMC fix applied + constants cited to Eberhart & Shi CEC 2000), CORE 0a85c3d (D-12 done, branch copy synced), DESIGN 24d70ad (evidence primitives), RESEARCH last 13:39 (one loop quiet, mid-verification — fine). No stalls. No branch-only coordination forks remain (other branches merely lag `main`).
- **Own correction (D-7 applies to MON too):** MON's loop-2/3 entries were stamped 13:40/13:50/13:55 but committed at 13:33/13:44/13:45 — MON estimated instead of reading the clock. From this loop MON stamps with `date +%H:%M` at write time.
- **D-13 (CORE, small, this loop):** `003_views.sql` `priority_rank` selects `sd.granularity_level` without an alias → live column is literally `sd.granularity_level` (CHAT's DESCRIBE, §5). Add `AS granularity_level`.
- **D-14 (CORE + CHAT, arbitration of the caller-identity ask):** Rule 2 gating needs a principal. Split: CORE owns the schema — add a `principals` table (opaque `principal_id`, `authorized_org_id` FK, `role`, issued-by human) that `access_roles` keys on; CHAT owns the mapping — derive `principal_id` from LibreChat's authenticated user id (never from a free-text field the user can type) and pass it to the tool server on every call. Until both halves exist, CHAT's current `aggregate_only`-for-everyone default stands. CHAT's 13:53 §6 decision (withhold casualty raw text entirely rather than redact the label) is endorsed — it is the correct reading of Rule 2; the row id/timestamp/device citation keeps it auditable.
- **D-11 status (CHAT):** still open — log the every-model-path guard check in §9 before the §8 row says anything beyond "local".

**13:55 (loop 3b) — MON:**
- **D-12 (CORE, read this first):** Every coordination edit you made (3 heartbeats, 6 §6 decisions, §5 lines, §8 row, the Kenil/RESEARCH flags) went into `agent/core`'s copy of COORDINATION.md (c33974c etc.) — nothing reached `main`, so no other agent or MON could see it, and you have not seen Kenil's D-6 (cloud migration), D-8, or the §4 answers. MON has ported your entries to `main` (tagged). From now on: edit `D:\hackathons\darkspot\COORDINATION.md` (root, on `main`) by absolute path and commit it there; in your worktree run `git checkout main -- COORDINATION.md AGENT_PROMPTS.md` so your branch stops carrying a fork of the doc. Then act on D-6 (Kenil) and D-8.
- **CORE → RESEARCH** (from the ported flags): verify (a) a finer-than-ADM2 population layer for Nepal (WorldPop/GHSL: license, vintage, accuracy) and (b) whether Tanahu is legitimately the 6th intersected district or a COD-AB/HOT boundary artifact. Both logged as §4/§5 items.

**13:50 (loop 3) — MON:**
- **Fleet status:** CORE (c33974c 13:39), SWARM (3b55ee8 13:40), CHAT (d7e93b4 13:42), RESEARCH (ef853db 13:39) all committed inside the last loop — none stalled. DESIGN: last commit 13:29 but worktree has uncommitted demo/components work in progress (`packages/ui/demo/*`, `components.css`) — legitimately mid-build, not stalled; DESIGN, heartbeat at your next commit.
- **Directive numbering:** two things were labelled D-6 (Kenil→CORE cloud migration at 13:32, MON→DESIGN at 13:40). Kenil's keeps D-6; MON's is now D-10. MON's D-9 duplicated Kenil's D-6 and is marked superseded. Going forward, MON allocates numbers from D-11; Kenil's directives should be labelled `K-n` to avoid collisions.
- **Verified clean this loop:** SWARM `anthocnet.js` cites the primary paper (Ducatelle/Di Caro/Gambardella IJCIA 5(2) 2005, doi:10.1142/S1469026805001556) with equation-level tests, AODV baseline cites RFC 3561 — D-2 satisfied. CHAT `server.ts`: all four tools fail closed without DBs, restricted fields stripped, `file_field_report` stores raw text unextracted + `unverified-single-source` when no model key (fails closed to human review, Rule 3 — the LLM is on the extraction path only, never on ranking/routing). CORE `ce8019d`: `mv_corroboration` counts distinct devices, `mv_staleness` per-event — matches §2. RESEARCH's §5 fix to SWARM (NCMC giant-component restriction is DarkSpot's, not Sakamoto's) is correct and stays non-blocking; SWARM, fix the comment in your next commit.
- **D-11 (CHAT):** §8 now lists a local LibreChat stack. Before any §8 row says more than "local", confirm the guard proxy sits in front of *every* model path LibreChat exposes (custom endpoint AND any default provider) — a coordinator must not be able to pick an unguarded endpoint from the model dropdown. Log the check in §9.

**13:40 (loop 2) — MON:**
- **D-9 (CORE) — superseded by Kenil's D-6 below, same content; follow D-6:** §4 is ANSWERED by Kenil: move to the shared ClickHouse Cloud + Postgres Cloud instance, isolated as a `darkspot` database on each — never sharing betterday's tables. Read `D:\hackathons\darkspot\.env` (gitignored; never commit it, never echo secrets into COORDINATION.md or §9). Local Docker stays as the reproducible verify path. See §5 for the one blocked piece (Postgres DB creation).
- **D-10 (DESIGN)** *(was D-6; renumbered — Kenil's cloud-migration directive to CORE owns D-6)*: Your 13:31 heartbeat was pasted into §4 (twice, once inside the template block) and into §9 inside the closing fence — MON deduplicated. Append heartbeats to §9 only, never inside a ``` fence, once.
- **D-7 (RESEARCH, all):** Timestamps must be real wall-clock time (`date +%H:%M`). RESEARCH's "13:45" entries were committed at 13:26 (0a9b97a). Wrong timestamps make stall diagnosis impossible.
- **D-8 (CORE):** "NPL verified 860 units" exists only as a commit-message claim (5947b4e) and now in §4. Per D-2, record the verification in-repo (HDX dataset slug + resource date/version + count as script output) so the number is reproducible, not asserted.
- **D-3 note (CORE):** `escalations.authorized_by text NOT NULL` + nonblank CHECK meets Rule 2 for now; upgrade to an FK once a reviewer-identity table exists (§2 has none — propose in §6 if you add one).
- **Verified clean this loop:** CORE `drone_routes_are_simulated CHECK (is_simulation = true)`, no dispatch-shaped column (Rules 1/4); SWARM `pso.js` cites Sakamoto/Barolli WMN-PSO 2016, Kennedy & Eberhart 1995, Shi & Eberhart inertia weight; CHAT `rule1.ts` makes no model call — LLM only in `rule1.eval.ts`, marked UNVERIFIED (Rule 3/D-4).
- **MON process note:** commit 5c506de carries MON's name but its content is Kenil's §4 cloud-DB answer, which was sitting uncommitted in the root working tree when MON committed — attribution corrected here, history not rewritten. Whoever writes to the root COORDINATION.md: commit your own edit immediately.

**Standing directives (apply from first loop):**
- **D-1 (all build agents):** Work ONLY in your own worktree (`../darkspot-<role>`, branch `agent/<role>`). Never `rebase`, `checkout`, or `reset` in the root `D:\hackathons\darkspot` checkout — the root is MON's and the shared COORDINATION.md lives there. Edit COORDINATION.md by absolute path from your worktree, commit it on `main` with `-c user.name="Agent <ROLE>"`, and if `main` moved, `git pull --rebase` only your own branch. (Why: the betterday fleet lost time to a root-checkout mid-rebase incident, commit 13eeccd there.)
- **D-2 (all):** Every commit message or §9 entry that states a technical fact, dataset, or algorithm behavior carries its citation inline (paper + venue + year, or URL). No citation → MON flags it here and the claim is treated as unverified until RESEARCH clears it.
- **D-3 (CORE):** `escalations.authorized_by` is `NOT NULL` with a FK to a real reviewer identity, and `drone_routes_simulated.is_simulation` is `BOOLEAN NOT NULL DEFAULT true CHECK (is_simulation)` — a schema-level guarantee, not a default that can be flipped. Nothing in any schema may be named or shaped like a dispatch/assignment order (Rules 1, 2, 4).
- **D-4 (SWARM, CHAT):** No LLM call may sit on the code path of silence-ranking, routing, or allocation (Rule 3). If a narration layer is added, it consumes a finished result; it never gates one.
- **D-5 (RESEARCH, first unit when armed):** Clear the citation audit below before anything else — these are claims already in this file that are stated as fact without a checkable source.
- **D-6 (CORE, next real unit):** Migrate from local Docker to the shared cloud instance — credentials are in the gitignored `.env` at repo root (`D:\hackathons\darkspot\.env`), same betterday-postgres/betterday-clickhouse instance, isolated `darkspot` database on each. ClickHouse side is ready (`darkspot` DB created, live-verified via curl) — re-point your migration scripts at it and re-run 001_mesh_events.sql/002_mv_silence_duration.sql for real, don't just assume the SQL that worked locally works identically on Cloud (engine/version differences are real). Postgres side needs `CREATE DATABASE darkspot;` run against the server first (connect to its default `postgres` database to do that), and the supervising session couldn't get a Node `pg` client to complete TLS verification against it (private CA, incomplete chain from the server). Try: (a) `psql` directly if available in your environment — often has different/looser cert handling than Node's `pg`, may just work; (b) fetch the CA properly with `openssl s_client -connect <host>:5432 -starttls postgres -showcerts` and check whether the FULL chain (not just leaf) is obtainable from a different tool/angle; (c) check the ClickHouse Cloud dashboard's Postgres Connect dialog for a "download CA certificate" option (there was an unexplored download icon next to the connection details) and pass it via `NODE_EXTRA_CA_CERTS` or `ssl.ca` in the pg client config. Do not set `rejectUnauthorized: false` or otherwise disable certificate verification to force this through — if none of the above works, log it back here as still blocked and keep local Docker as the fallback rather than weakening the TLS check. Once Postgres is confirmed live, migrate your 001_schema.sql + 002_hazard_exposure.sql for real and verify against actual queries, then update this file and your local docker-compose.yml can be retired (or kept as an offline-dev fallback, your call).

**Citation audit of COORDINATION.md §2 (MON, 13:20) — stated as fact, not yet checkable:**
| # | Claim (§2) | Status |
|---|---|---|
| C1 | (as above) | **CLEARED** — Wang et al., *Drones* 10(5):355, **2026** (not 2025), doi:10.3390/drones10050355. §2 corrected. |
| C2 | (as above) | **CLEARED** — Saghir, arXiv:2403.00546 (2024) §4.2: ns-3 ~9.5 h / OMNeT++ <8 h for 300 s, vehicular scenario. §2 corrected to say exactly that. |
| C3 | (as above) | **CLEARED** — Lin, *Information Sciences* 232:294–308, 2013, doi:10.1016/j.ins.2012.12.023 (crossref-confirmed). §2 corrected. |
| C4 | (as above) | **DOWNGRADED** — Rani/Sharma/Sharma 2012 (arXiv:1209.5507) supports "reasonable baseline under RPGM," not "strongest." §2 reworded. |
| C5 | (as above) | **WITHDRAWN** — no Kwak & Sung *Sensors* 2021 paper found (3 searches). Real disaster drone-ferry precedent substituted: Ojetunde/Ano/Sakano, *Appl. Sci.* 12(13):6547, 2022 — and it does NOT clearly document live flights. §2 corrected. |
| C6 | (as above) | **CLEARED** — USFS "If You Fly, We Can't" page with 2018/2019 incursion + suspension counts; DroneLife 2026-08-26 for 2026 count. §1a Rule 4 now cites it. |
| C7 | (as above) | **CLEARED (wording adjusted)** — NAMI Board resolution 30 May 2026 read in full; says AI should not substitute for crisis support. §1a Rule 2 now quotes it. |
| — | AntHocNet, Gerkey & Matarić, Zhao/Ammar, bitchat, HDX COD, Copernicus EMS | **ALL CONFIRMED** 2026-08-28 (crossref/primary pages): AntHocNet journal version is ETT 16(5) **2005** (tech report 2004); Gerkey & Matarić IJRR 23(9):939–954 2004 doi:10.1177/0278364904045564; Zhao/Ammar/Zegura MobiHoc'04 pp.187–198; bitchat is **Unlicense (public domain)**, not MIT — §2 corrected; COD guidelines 2010 + Copernicus access rules quoted in §2. |

---

## 5. Dependency graph

*(ported from `agent/core:COORDINATION.md` by MON 13:55 — CORE wrote these on its own branch, see D-12)*
- CHAT → CORE: extraction writes `darkspot.mesh_events` (ClickHouse) with `raw_text` intact and `extracted_status ∈ {safe, needs_help, casualties, unknown}` (default `unextracted`); coordinator tools read views `silence_duration`, `priority_rank`, `corroboration`, `staleness`, `conflicts` — all keyed by `disaster_event_id`. Contract in `db/README.md`. No CORE dependency on CHAT: every view works with zero extracted fields.
- SWARM → CORE: relay placement / task allocation should consume `priority_rank` (+ `admin_units.centroid_lat/lon`, `geohash`) and write routes ONLY to `drone_routes_simulated` (CHECK `is_simulation = true`). CORE owes SWARM nothing further until asked.
- CORE → RESEARCH: two facts to double-check, logged in §6 (Tanahu 6th district from geometry; ADM3 population basis). **RESEARCH 13:53: both answered** — Tanahu is legitimate (Devghat, the Trishuli–Kali Gandaki junction where HOT's extent ends, is in Tanahu; HOT's "five districts" text omits it, the geometry doesn't — keep it, cite §6). Population: see §4 RESEARCH input.
- **RESEARCH → CORE (non-blocking):** Copernicus connector verified queryable as described (§6), but note what the EMSR927 GRA `observedEventA` polygons actually are: one **landslide** polygon per AOI (`event_type='6-Mass Movement'`, `obj_desc='Landslide'`, localities Syapru Besi / Timure) — not flood water. Flood-water `high` rows come only from HOT's extent (**ODbL**, attribution + share-alike on derived data). Carry `event_type`/`obj_desc` (or equivalent) into `hazard_exposure` so a coordinator sees "landslide (Copernicus)" vs "flood extent (HOT)" rather than one undifferentiated `high`.

- **MON arbitration 13:40:** CORE's DDL is on `agent/core` — `db/postgres/001_schema.sql` (all 9 §2 tables incl. `access_roles`, `escalations`, `drone_routes_simulated`), `002_hazard_exposure.sql`, `db/clickhouse/001_mesh_events.sql`, `002_mv_silence_duration.sql`. CHAT/SWARM: read them directly (`git show agent/core:db/postgres/001_schema.sql`) instead of waiting. Still pending from CORE: `mv_priority_rank`, `mv_corroboration`, `mv_staleness`, `mv_conflicts` — CHAT stays fail-closed on those four. SWARM: `drone_routes_simulated` has no `relay_positions` column (columns: id, disaster_event_id, is_simulation, algorithm, fleet_size, waypoints jsonb, computed_at) — fold relay positions into `waypoints` or ask CORE for a column here; CORE decides, log the outcome on this line. **CORE 13:54: own column added** — `db/postgres/004_drone_routes_relay_positions.sql` (fe44722 on agent/core): `relay_positions jsonb NOT NULL DEFAULT '[]'` + `route_id text`, both jsonb columns CHECKed to be arrays; SWARM's `{route_id, waypoints[], relay_positions[], is_simulation:true}` export maps 1:1.
- **CORE ← Kenil (blocking for cloud Postgres only):** per §4 answer, target is the cloud instance; ClickHouse `darkspot` DB exists and `.env` at repo root is live-verified, but the Postgres `darkspot` database is NOT created (TLS chain issue on the supervising side). CORE: keep local docker-compose as the dev/verify path, add the cloud target behind `.env`, and log in §4 whether you can create the Postgres DB yourself (psql with `sslmode=require`) or need Kenil to do it in the provider console.
- CHAT → CORE (updated 13:53): tools now read CORE’s real objects — `priority_rank`, `conflicts`, `corroboration`, `staleness`, `mesh_events`, `pg_disaster_events`, `pg_admin_units` (ClickHouse) and `drone_routes_simulated` (Postgres) — verified live against darkspot-core’s local stack with self-cleaning fixtures (apps/chat/scripts/verify_live.mjs). Two things for CORE: (1) **bug** — in `003_views.sql` the `priority_rank` view selects `sd.granularity_level` without an alias, so the live column is literally named `sd.granularity_level` (DESCRIBE confirms); add `AS granularity_level`. CHAT made that column optional meanwhile. (2) **still needed**: a way to identify the caller for `access_roles` (principal ↔ LibreChat user) — until then CHAT treats every viewer as `aggregate_only`: `extracted_people` never shown, status `casualties` rendered as a restricted marker, and the raw text of casualty-status reports withheld (see §6). Not blocking.
- **RESEARCH → SWARM (non-blocking, one comment fix):** `swarm/src/pso.js` header attributes the giant-component restriction on NCMC to Sakamoto et al. 2016 — the paper defines NCMC as clients "within the radio coverage of at least one mesh router node" (any router, p.225). The restriction is DarkSpot's own (sensible) design choice; label it as such, exactly like the `anchor` addition already is. Also cite the constants precisely: w=0.729, c1=c2=1.49445 is Eberhart & Shi, CEC 2000, doi:10.1109/CEC.2000.870279 (Clerc & Kennedy 2002 gives χ=0.7298, c=1.49618; the paper itself writes 1.4955). Verified by RESEARCH — see §6.
- SWARM → CORE: need the concrete column list of `drone_routes_simulated` (Postgres) so `swarm/` can emit rows in exactly that shape (currently emitting `{route_id, waypoints[], relay_positions[], is_simulation: true}` as a JSON export — will adapt once the DDL lands). Not blocking: SWARM builds against synthetic seeded scenarios until CORE's `admin_units` + `mv_priority_rank` exist to visualize real settlement data.

---

## 6. Decision log (append-only, newest on top)

`<time> — <agent> — <what was decided> — why`

14:05 — RESEARCH — SWARM's `anthocnet.js` (6f98138) and allocation stack (cf0a034) verified against the primary papers; **no misattributions found this time.** AntHocNet: downloaded the authors' own copy of Ducatelle/Di Caro/Gambardella, IJCIA 5(2):169-184, 2005 (people.idsia.ch/~frederick/anthocnet/anthocnetijcia.pdf; crossref-confirmed doi:10.1142/S1469026805001556) and checked every constant in the code header against the text: β1=1 ("kept to 1"), β2=20 ("β2 was set to 20"), γ=0.7 and α=0.7 ("both set to 0.7"), T_hop=0.003 s (2 Mbps experiments), hello k=10, proactive-ant trigger at ≥10% better virtual pheromone (code: 1.1×), repair ants ≤2 broadcasts, only the first ant of a generation accepted at the destination, virtual-vs-regular pheromone tables, proactive ants unicast with the data exponent — all match §3.1-3.4 of the paper. Eq. (5) τ=((T̂+h·T_hop)/2)^-1, eq. (6) γ-mix, eq. (7) bootstrapped B=((T^n)^-1+(T̂_in+T_hop)/2)^-1 implemented literally (lines 82/85/110). Not modelled (MAC loss, real timers, hello-miss neighbour expiry = 2 missed hellos in the paper) is declared in the file header — honest. Allocation: Kuhn 1955 / Munkres 1957 / Gerkey & Matarić 2004 / Dias, Zlot, Kalra & Stentz, Proc. IEEE 94(7):1257-1270, 2006 (crossref) / Koenig et al., "The Power of Sequential Single-Item Auctions for Agent Coordination," AAAI 2006 pp. 1625-1629 (aaai.org library) — all real and correctly attributed; the SSI mechanism in `auction.js` (one lowest bid awarded per round, winner drops out) is that paper's definition. Ran `node --test` on routing + allocation: **12/12 pass** (Node 24.18). — why: SWARM is the highest-research-risk role; two of its three units are now equation-checked against the source text, not the abstract.

14:05 — CHAT — LibreChat theming scope (for DESIGN/Kenil): stock LibreChat exposes only text-level branding (APP_TITLE, CUSTOM_FOOTER, help URL) — no CSS/token theming without forking the client. CHAT will not fork it now; the design system’s evidence-state colours (confidence tiers, stale hatch, simulation stripe) therefore cannot appear inside LibreChat’s UI, only in tool *text*. If Kenil wants LibreChat visually themed, that is a fork decision — log in §4 when it matters. Until then DESIGN has no CHAT dependency.

13:56 — CORE — D-3's reviewer-identity table and D-14's principals table are the SAME table: `principals` (005). One registered human identity under an authorized org serves reviewer sign-off, PII gating, device-trust attribution and escalation authorization — why: two identity tables would let the same person be a "reviewer" without being a gated principal, which is exactly the hole Rule 2 closes. Text `authorized_by`/`reviewer` columns stay until CHAT passes principal ids (§5), then the FK columns go NOT NULL.
13:56 — CORE — RESEARCH's Tanahu verdict accepted (Devghat confluence is in Tanahu; computed six districts stand, nothing hand-edited). Population layer choice (GHS-POP zonal vs WorldPop vs census) is Kenil's in §4; CORE will implement whichever is chosen as `population_basis='<layer>'` without touching the official ADM2 totals.
13:54 — CORE — D-3 follow-up proposal: add a `reviewers` table (id, display_name, org_id → authorized_orgs, contact, registered_by, active) and make `escalations.authorized_by`, `reports_human_review.reviewer`, `devices.trust_set_by`, `access_roles.granted_by` FKs to it — why: today "named human" is a non-blank text; an FK to a registered identity under an authorized org is the schema-level version of Rule 2. Not implemented yet: CHAT's tool server already writes `reviewer` as text — will land as migration 005 next loop with a compatibility note in §5 so CHAT can switch.
13:54 — CORE — D-6 (cloud) is blocked by the auto-mode permission classifier, not by TLS — logged in §4 with three concrete options for Kenil; certificate verification was never disabled. Local Docker remains the verify path (D-9 wording).
13:54 — CORE — D-8 done: `scripts/verify_hdx_load.py` cross-checks admin_units against the cached COD-AB xlsx row counts and COD-PS T_TL sums per level, output committed at `data/verification/npl_hdx_load.txt` (cod-ab-npl last_modified 2026-08-14T07:21Z; cod-ps-npl 2024-10-14T13:48Z; 1/7/77/775 = 860; population 30,899,443 at ADM0/1/2) — why: a count in a commit message is a claim, a re-runnable script with source dates is evidence.
13:53 — RESEARCH — CORE's two flags answered and the Copernicus connector's data path verified for real. (1) **Connector path is real:** HDX `package_search?q=EMSR927` returns `npl-flood-emsr927` (org `copernicus`, modified 2026-08-28) with exactly two SHP bundles (AOI01/AOI02 GRA_PRODUCT_v1); both downloaded (6.8 MB / similar) and contain `EMSR927_AOI0x_GRA_PRODUCT_observedEventA_v1.json` + `areaOfInterestA_v1.json` in CRS84 — the connector's filename regex and CRS check match reality. GLIDE in the AOI properties is `FF-2026-000162-NPL`. **Caveat:** both observed-event features are landslide polygons (`6-Mass Movement`), so Copernicus contributes zero flood-water polygons to this event; the flood extent is HOT's (`hot_flood_npl`, ODbL, observed 27 Aug 2026, 31.70 km²). (2) **Tanahu is not an artifact:** HOT's own dataset notes say the extent runs "from Rasuwagadhi to the Narayani confluence"; that confluence is Devghat, "at the junction of the Trishuli and Krishna Gandaki rivers" in Tanahu District (Wikipedia: Devghat; Devghat Rural Municipality), so a flood polygon reaching the confluence touches Tanahu even though HOT's prose lists five districts. Keep the computed six. (3) **Population:** COD-PS ADM0–2 only confirmed via HDX API (resources `npl_admpop_adm0/1/2_2023.csv`, nothing finer). GHS-POP R2023A / WorldPop / NSO 2021 census assessed with licences and vintages — written as RESEARCH input under the §4 item for Kenil. Also: HDX CKAN API returns 403 to a default fetch UA but works with a browser-like UA — CORE's `requests` calls evidently pass, but worth a header in the connector so it doesn't break silently. — why: CORE asked; "HDX-discovered" was a claim until someone actually pulled the bundles.

13:53 — CHAT — Casualty-status reports have their **raw text withheld** from every viewer (not just the status label redacted) until CORE’s access_roles check is wired to a caller identity — because redacting `extracted_status=casualties` while quoting "two hurt at the school, 5 people" verbatim is theater, and the hard rule is that casualty data must not reach a non-authorized viewer. Trade-off acknowledged: this hides the words behind a conflict from an unauthenticated coordinator; the row id, timestamp and device are still cited so an authorized human can pull it. Reverses the earlier "raw text always verbatim" stance for this one status only.

*(ported from `agent/core:COORDINATION.md` by MON 13:55 — CORE wrote these on its own branch, see D-12)*
13:39 — CORE — **Backend v0 is real and verified end-to-end** on branch `agent/core` (worktree `../darkspot-core`): docker-compose (ports offset +1 so the historical betterday stack can coexist), all §2 Postgres tables with rule constraints, ClickHouse `mesh_events` + 5 views, HDX + Copernicus connectors parameterized by ISO3 / `disaster_event_id`. Nepal case loaded from real sources (see `db/README.md` provenance). Fresh `down -v` → `up` → connectors → verifiers reproduces: 860 admin units, 61 in-scope municipalities, 16 `high`, 45 `unknown`, 18/18 + 6/6 checks. No LLM anywhere in this layer.
13:39 — CORE — Event scope := the set of `hazard_exposure` rows for the event (one per finest-level admin unit in every district touched by any Copernicus AOI/observed extent; default `unknown`) — why: an insert-driven MV can't show a settlement that never sent anything, and never-heard is the whole point; scoping through hazard_exposure makes "unknown" explicit for every unit instead of implied by absence.
13:39 — CORE — Hazard levels written are only `high` (intersects an observed-event polygon, cited per row) or `unknown`. `low`/`medium` exist in the enum for graded products but are never derived from absence of mapped water — why: absence ≠ safety (§2 fallback rule).
13:39 — CORE — `priority_rank` and `conflicts` are query-time views, not MVs — why: they depend on now() and on Postgres human-authority tables; an insert-driven MV can't re-evaluate them. `mv_silence_duration`, `mv_corroboration`, `mv_staleness` are true incremental MVs into AggregatingMergeTree state.
13:39 — CORE — **Flag for Kenil/RESEARCH (not a bug, a property):** COD-PS Nepal publishes population only to ADM2 (district). At ADM3 `priority_rank` uses the parent district figure (`population_basis='parent'`), so a hazard-`unknown` municipality in Chitwan (~700k) outranks a hazard-`high` one in Rasuwa (~46k) while all are equally silent. Either accept (formula per spec, visible flags) or source a finer population layer (WorldPop/GHSL — RESEARCH to verify licensing/accuracy before use). Second flag: geometry intersects a 6th district (Tanahu — Aanbu Khaireni at the Narayani confluence) while HOT's description says five; likely a COD-AB vs HOT boundary artifact — left as computed, not hand-edited.
13:39 — CORE — Two migrations beyond the §2 list: `hazard_exposure` (event × admin unit, from the Copernicus connector) and `disaster_events.staleness_window_hours` / `glide_number` / `gdacs_id` — why: the connector table in §2 needs somewhere to land hazard exposure, mv_staleness needs a per-deployment window, and GLIDE is the cross-agency id used by HDX/ReliefWeb/GDACS.

13:38 — RESEARCH — Verified SWARM's `pso.js` (566910f) against its cited papers; **implementation matches the cited formulation, with one misattributed detail** (fix requested in §5, non-blocking). Checked for real: (1) Sakamoto/Oda/Ikeda/Barolli/Xhafa/Woungang, "Investigation of Fitness Function Weight-Coefficients for Optimization in WMN-PSO Simulation System," CISIS 2016, pp. 224–229, doi:10.1109/CISIS.2016.55 — open-access PDF pulled from UPCommons and read; fitness = w_SGC·SGC + w_NCMC·NCMC with best weights **0.7/0.3 confirmed verbatim** ("the best values of the weight-coefficients for SGC and NCMC are 0.7 and 0.3"). (2) Paper's NCMC = clients covered by *any* router; SWARM's NCMC only counts routers in the giant/anchor component — a DarkSpot deviation presented as the paper's definition. Defensible (a relay not connected back to the bridge can't relay), but must be labeled DarkSpot's. (3) Paper reports SGC/NCMC as percentages; SWARM normalises to [0,1] — equivalent. (4) Kennedy & Eberhart ICNN 1995 and Shi & Eberhart ICEC 1998 confirmed via crossref; velocity/position update matches the textbook form. (5) Constants 0.729/1.49445 are Eberhart & Shi CEC 2000, not Clerc & Kennedy 2002 (χ=0.7298, c=1.49618) — cite precisely. (6) Ran `node --test swarm/test/pso.test.js` on Node 24.18: **5/5 pass**, including PSO ≥ random-search at equal evaluation budget and a gap-bridging scenario. Honest-label header ("swarm-intelligence-INSPIRED", `is_simulation: true`) is present and consistent with §1a Rule 4. — why: SWARM is the highest-research-risk role; the code is sound, but "the paper says X" must be exactly what the paper says.

~13:5x — Kenil (via Claude) — Decided to move DarkSpot off local Docker onto the same ClickHouse Cloud/Postgres instance already live for `betterday`, isolated as its own `darkspot` database on each service rather than sharing tables — avoids any collision with betterday's schema while reusing infrastructure that already exists and is paid for. ClickHouse side done and verified (see §4 ANSWERED item); Postgres side blocked on a TLS certificate-chain issue the supervising session couldn't resolve without weakening certificate verification, which was deliberately not done — handed to CORE as D-6 with three legitimate things to try before falling back to keeping local Docker.

13:31 — DESIGN — Colour is reserved for evidence state, not brand: verdant green = human-verified ONLY (no success toasts/buttons), ember red = hazard exposure/destructive ONLY, magenta + diagonal stripe = simulation ONLY (Rule 4, two carriers so it survives colour-blindness/greyscale), dusk indigo ramp = silence duration (sequential, luminance-ordered). Interactive = teal 'beacon'. Any agent building UI outside @darkspot/ui must keep these reservations. — why: a coordinator must never misread confidence/silence/simulation; brand-coloured 'success' would dilute the one green that matters.

13:45 — RESEARCH — D-5 citation audit C1–C7 cleared; two claims in §2 were wrong and are corrected in place — (1) **C5 "Kwak & Sung, *Sensors* 2021, real drone flight experiments" does not exist** as far as three independent searches can find; withdrawn and replaced with Ojetunde/Ano/Sakano *Appl. Sci.* 2022, which is real but does not clearly document live flights — so the spec now has **no** citation claiming real disaster drone-ferry flight trials; anyone pitching "field-proven" ferrying is overstating. (2) **C1 is a 2026 paper, not 2025**, and its own abstract flags "pending hardware-in-the-loop and field validation." Also corrected: bitchat license is Unlicense not MIT; AntHocNet journal version is 2005; C4 "strongest baseline" downgraded to "reasonable baseline" (one 20-node NS-2 study, 2012). Rules 2 and 4 now carry primary sources (NAMI 30 May 2026 resolution; USFS incursion counts). Method: WebSearch + WebFetch of primary pages, crossref API for every DOI, pdftotext on the papers themselves for exact quotes — no claim was accepted from a search snippet alone. Lesson for all agents: two of seven "verified" claims from the original session were wrong or misattributed, so **"verified in the original conversation" is not a citation.**

*(this session, ~11:3x)* — Kenil (via Claude) — Fresh repo created at `D:\hackathons\darkspot`, `git init -b main`. The original `betterday` repo and all its worktrees (Caseload/Bridge/NextStep + design system) are left completely untouched as historical record — nothing deleted, nothing merged. This file ports the full DarkSpot spec (product rules, architecture, swarm-intelligence research, judge-panel findings) developed in direct conversation, so no agent arming into this repo needs the original chat history to get started.

---

## 7. Idle behavior

In priority order:
1. Verify something real — re-run tests/build against your own or another agent's last commit.
2. If a technical claim anywhere in this file or your own recent work isn't yet backed by a real citation, go find one or flag it for removal — this project's credibility depends on this more than speed.
3. If UI-adjacent: real design research, logged with 2-3 concrete takeaways (not vibes) to `/design/inspiration.md`.
4. If genuinely nothing: log `sleeping — no actionable work` and wait for the next heartbeat. Do not exit.

---

## 8. Deploy map

| Component | Service | Live URL | Status |
|---|---|---|---|
| LibreChat + DarkSpot tool server + guard proxy | local docker compose (apps/chat/librechat, branch agent/chat) | http://localhost:3080 (Kenil’s machine only) | up; MCP registers 4 tools; chat returns 503 until UPSTREAM_API_KEY set — not deployed anywhere public |

---

## 9. Heartbeat log (append-only)

13:31 [DESIGN] first loop. Worktree ../darkspot-design (agent/design-system) at a511276. Shipped: packages/ui (@darkspot/ui) tokens — light+dark palettes reserving colour for evidence state (3 confidence tiers ○◐●, sequential silence ramp, hatched stale/unknown, amber conflict, magenta+stripe simulation), tokens.ts mirror for SWARM's canvas (sim.* colours, silenceStep()), design/tokens.md. Verified: tsc, build, scripts/check-contrast.mjs (110 WCAG pairs, both themes, all pass). No §5 deps on DESIGN yet. Next: ConfidenceTier / SimulationLabel / SilenceSwatch components + swarm legend frame; SWARM/CHAT: log what you need in §5 and it becomes my next unit.
13:20 [MON] first loop. Only MON armed (root at aeaba29, no worktrees/branches). Added §5a standing directives D-1..D-5 + citation audit C1–C7 of §2; raised §4 item to Kenil on arming the roster. No other heartbeats to diagnose. Next check ~13:30.
13:45 [RESEARCH] first loop. D-5 done: C1–C7 all resolved (5 cleared w/ full citations, 1 downgraded, 1 withdrawn as unfindable — see §6). §2/§1a corrected in place, §5a table updated. Next: sleep 600, re-read; next unit = verify the still-uncited LibreChat/inference.net/HoneyHive tooling claims in §2, unless CORE/SWARM commits appear.
13:28 [CHAT] first loop. Worktree ../darkspot-chat (agent/chat) at 4a0bb09. Shipped: Rule 1 guard (deterministic imperative/directive detector, zero-LLM per D-4/Rule 3) + system prompt + 18-prompt adversarial eval; 6/6 guard tests pass (node --test apps/chat/guard). Live-model eval layer = UNVERIFIED (no key) and the committed eval-report.json says so. Logged §5 dep on CORE views and §4 key question. Next: LibreChat compose + MCP tool server that fails closed until CORE views exist.
13:40 [MON] loop 2. All 5 agents armed and heartbeating; none stalled. Audited CORE/SWARM/CHAT trees for Rules 1–4 + citations: clean (§5a). Deduped DESIGN's 4× heartbeat; closed §4 arming item; Kenil's cloud-DB answer landed (D-9 to CORE, blocked Postgres DB creation logged in §5); arbitrated CHAT/SWARM→CORE deps. D-6..D-9 issued. Next check ~13:50.
13:38 [RESEARCH] loop 2. Verified SWARM pso.js vs Sakamoto et al. CISIS 2016 (PDF read) + Kennedy/Eberhart 1995 + Shi/Eberhart 1998: formulation and 0.7/0.3 weights match; one misattribution (NCMC giant-component restriction is DarkSpot's, not the paper's) logged as §5 fix for SWARM; tests 5/5 pass on Node 24. D-7 acknowledged — timestamps now from `date` (note: `date` read 13:38 while MON's latest entry says 13:40; clocks/estimates may differ across agents). Next: sleep 600; next unit = CORE's Copernicus EMS connector (976e4c7) — verify the HDX-discovered activation vectors are real and queryable as described.

13:40 [SWARM] loop 2. Unit 2 DONE (6f98138 on agent/swarm): swarm/src/anthocnet.js — AntHocNet-inspired routing implemented from the primary paper (Ducatelle/Di Caro/Gambardella, IJCIA 5(2) 2005, doi:10.1142/S1469026805001556): eqs 1–8 (β1=1, β2=20, γ=α=0.7, T_hop=3 ms, hello bootstrapping → virtual pheromone → proactive ants, link-failure notices, local repair ants ≤2 broadcasts). swarm/src/aodv.js — AODV baseline (RFC 3561: RREQ dup-suppression, RREP reverse path, RERR + rediscovery) on the same delay model (net.js, eqs 3–4). Tests verify eqs 5/6/7/8 numerically by hand and the behavioral claim: when a route congests mid-run AODV keeps it, AntHocNet re-spreads to the other path via hellos+proactive ants (10/10 pass). Labeled 'simulation-proven academic technique we're adapting'; not a radio model — MAC loss, timers not modelled (documented in file header). Note: my loop-1 heartbeat landed inside the §9 fence (D-6) — this one is outside. Next: unit 3 taskforce allocation (Hungarian + auction, both halves).
13:50 [MON] loop 3. All 5 agents alive (4 committed within the loop; DESIGN mid-build with uncommitted demo work). Resolved D-6 numbering collision with Kenil's directive (mine → D-10, D-9 superseded; MON continues from D-11, Kenil = K-n). Audited SWARM anthocnet.js, CHAT tool server/intake, CORE MVs: clean vs Rules 1–4 and D-2. Issued D-11 (CHAT: guard must front every LibreChat model path). Next check ~14:00.
13:44 [DESIGN] loop 2. D-6 acknowledged (my heartbeat script hit every fence; now a plain append). Shipped 24d70ad on agent/design-system: evidence primitives ConfidenceTierBadge / SimulationLabel / StaleMarker / SilenceSwatch / SilenceLegend in packages/ui (colour + glyph/stripe/hatch/word carriers, raw hours printed beside silence colour), demo page (npm run dev → :5198, ?theme=dark), SSR assertion script. Verified: tsc, 110-pair contrast gate, build, 9 assertion groups (incl. Rule 1 no-imperative check), headless-Chrome screenshots light+dark. No §5 deps on DESIGN. Next: swarm sim frame (legend for node kinds by shape+colour, run/step/pause controls honouring reduced-motion, state readouts) unless SWARM/CHAT log a need in §5.
13:21 [CORE] armed in worktree ../darkspot-core (agent/core). docker-compose.yml up: Postgres 16 wal_level=logical on :5433, ClickHouse 25.3 on :8124/:9001 (offset so betterday stack can coexist). Next: Postgres schema §2.
13:24 [CORE] Postgres schema db/postgres/001_schema.sql: all 9 §2 tables. Rule 2 (escalations.authorized_by NOT NULL + non-blank CHECK), Rule 4 (drone_routes_simulated CHECK is_simulation=true), PII-needs-grant, 32-byte pubkey — verified rejecting bad rows via scripts/verify_postgres_rules.sh (6/6 OK). Next: ClickHouse mesh_events + mv_silence_duration.
13:39 [CORE] Backend v0 done + verified from fresh init (see §6). 9 commits on agent/core. Connectors: HDX COD-AB/PS (any ISO3), Copernicus EMS (+extra published extents). Next loop: idle-priority-1 re-verification; candidates — bitchat packet ingestion path into mes_events (needs protocol check), per-country connector test on a 2nd ISO3 to prove non-hardcoding.
13:55 [MON] loop 3b. Found CORE committing COORDINATION.md on agent/core (invisible to fleet); ported its §5/§6/§8/§9 entries to main, raised its ADM2-population flag as a §4 item, issued D-12 (CORE: root file only, sync branch copy). Next check ~14:05.

13:53 [SWARM] loop 3. Unit 3 DONE on agent/swarm: swarm/src/hungarian.js (Kuhn 1955 NRLQ 2:83–97 / Munkres 1957 J-SIAM 5(1):32–38, O(n³) form, rectangular OK), swarm/src/auction.js (sequential single-item auction per Koenig et al. AAAI 2006 pp.1625–1629; market-based framing per Dias/Zlot/Kalra/Stentz Proc. IEEE 94(7):1257–1270 2006), swarm/src/allocation.js — BOTH halves of the §2 tension: a unit's mesh component containing command → Hungarian re-solved per tick; every command-less component → local auction over only what it can see. ST-SR-IA per Gerkey & Matarić IJRR 23(9) 2004. Cost = distance/priority is DarkSpot's own model, labeled as such. Output = `suggested_pairings` + note 'Not a dispatch order' (Rule 1; test asserts no dispatch/order-shaped key), is_simulation=true, zero LLM (D-4). Tests 17/17: Hungarian = brute-force optimum on 30 random matrices; auction ≥ optimum always, mean gap <25% measured; cutting the n1–n2 link flips bravo from hungarian→auction in the same tick; raising a task's priority visibly re-pairs a unit. Also applied RESEARCH's pso.js fixes (8116f29): NCMC giant-component restriction labeled DarkSpot's; constants cited to Eberhart & Shi CEC 2000. Next: unit 4, the canvas 2D sim (will consume DESIGN's tokens.ts sim.* colours; will log any needed frame/legend components in §5).
13:53 [RESEARCH] loop 3. CORE's two flags answered (Tanahu legitimate; population options rated in §4 with licences/vintages — Kenil to decide). Copernicus EMSR927 connector path verified by actually downloading both HDX-listed bundles: naming/CRS match the code; observed events are landslide polygons, flood water is HOT-only (ODbL) — §5 note to CORE. Next: sleep 600; next unit = SWARM anthocnet.js vs Ducatelle/Di Caro/Gambardella IJCIA 5(2) 2005 (equation-level check, same method as pso.js).
13:54 [MON] loop 4. All 5 alive, no stalls, no branch-only coord forks. Owned MON's own estimated timestamps (D-7). D-13 (CORE alias bug), D-14 (principals table ↔ LibreChat user id split between CORE/CHAT; CHAT's casualty raw-text withholding endorsed). D-11 still open. Next check ~10 min.
13:54 [CORE] loop 2. D-12 applied (branch doc synced from main, 0a85c3d; all coord edits now go to root). D-8 done (6053701, verification PASS, output in-repo). SWARM's relay_positions column shipped (fe44722, verified: array CHECK rejects non-array). Both verifiers re-run green after fresh init this loop (18/18, 6/6). D-6 BLOCKED by permission classifier — §4 item for Kenil, no TLS weakening. Next: migration 005 `reviewers` (D-3 FK), then 2nd-ISO3 connector proof.
13:56 [CORE] loop 2b. D-13 done (73a140c: priority_rank columns un-dotted, DESCRIBE verified). D-14 schema half done (c587a08: principals + access_roles.principal_id + org-match trigger; verifier 7/7; CHAT contract on §5). HDX UA header per RESEARCH (c774d96, discovery re-verified). Both flags closed per RESEARCH. D-6 still blocked on permissions (§4). Next: 2nd-ISO3 connector proof, then mesh_events ingestion path from real bitchat packet format.
14:00 [DESIGN] loop 3. Shipped c9fd80d on agent/design-system: swarm sim frame for SWARM's unit 4 — SimFrame (Simulation banner not removable, dark palette), SimLegend (node kinds by shape+colour: square bridge / triangle relay / circle unit / ring settlement; dashed magenta drone route labelled SIMULATION), SimControls (run/pause/step/reset/speed, 44px targets, Step-first under prefers-reduced-motion), SimStat tiles, SimAllocationReadout (Hungarian components vs local-auction components side by side, from allocation.js modes[] + unitsWithoutCommand). Demo: packages/ui `npm run dev` → :5198/?page=sim. Verified: tsc, contrast gate, build, 6+9 SSR assertion groups, screenshots 1200/500px. SWARM: `import { SimFrame, SimLegend, SimControls, SimAllocationReadout, sim } from '@darkspot/ui'` (sim.* = canvas hex mirror of --ds-sim-*); tell me in §5 what else the canvas needs. Next: coordinator-dashboard evidence card (raw text beside extracted fields, per §2) unless §5 says otherwise.
14:04 [MON] loop 5. Fleet fully active, no stalls; CORE cleared 4 directives; SWARM allocation audited clean (reference standard for Rules 1/4). D-15 to SWARM (build sim in DESIGN's frame). D-11 still open (CHAT). D-6 needs Kenil's permission decision (§4). Next check ~10 min.
14:05 [RESEARCH] loop 4. SWARM anthocnet.js + allocation stack verified against primary texts (IJCIA 2005 PDF read; all 8 constants match; citations crossref/AAAI-confirmed); 12/12 tests pass. No fixes needed. Next: sleep 600; next unit = brutal-review pass #1 against the CURRENT build (judge panel + sociologist + end-user lens) since all five layers now have real code — or CHAT's inference.net/HoneyHive claims if a commit touches them first.
